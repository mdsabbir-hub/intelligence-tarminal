Place your master Apple Numbers file here as `database.numbers`.

This exact filename/path (`source/database.numbers`) is what the parser and
the GitHub Action both look for - keep it as-is, or update the path in both
`.github/workflows/update-data.yml` and `scripts/numbers-parser.py` if you
rename it.
